# 松贴（Songtie）技术实现计划

> 目标：以可独立验证的纵向切片逐步交付单机桌面应用；每一阶段都保持程序可运行、数据可保存，并在通过明确验收后才进入下一阶段。

## 1. 已确定约束与计划假设

已确定的技术约束：

- 唯一目标平台为 Ubuntu 22.04 / Linux x86_64；
- 产品只考虑一台电脑、一个本地用户、一个应用实例；
- 不为未来的移动端、同步、协作或插件系统预留抽象；

当前计划采用以下工作假设：

- 首版使用 Tauri 2、React、TypeScript、Vite、SQLite；
- 绘图能力优先验证 `@excalidraw/excalidraw`，但必须隔离在适配层中；
- 包管理器统一使用 pnpm；
- 技术细节服从 `product.md` 中的产品哲学。

## 2. 命名建议

### 2.1 中文名

正式中文名：**松贴**。

暂定标语：**把想法贴下，让脑子松开。**

### 2.2 英文名

推荐英文品牌名：**Songtie**。

不建议把 `easy-tag` 作为公开英文品牌名，原因是：

- `tag` 通常表示分类标签、追踪标签、吊牌或价格牌，不自然地指向 sticky note；
- 名字容易让人误以为产品核心是标签管理；
- 市面上已经存在多个名为 Easy Tag / EasyTag 的应用和软件；
- `easy` 与 `tag` 都很通用，搜索和品牌识别度较弱。

`easy-tag` 可以保留为内部快捷记录模块的临时代号，但代码仓库、应用标识和数据目录统一使用 `songtie`，避免后续迁移内部标识。

## 3. 技术路线

### 3.1 推荐技术栈

| 层级     | 选择                                               | 所有职责                                               |
| -------- | -------------------------------------------------- | ------------------------------------------------------ |
| 桌面壳   | Tauri 2 / Rust                                     | 窗口、托盘、全局快捷键、开机自启、单实例、应用生命周期 |
| 界面     | React + TypeScript + Vite                          | 墙面、随手贴、检视模式、完成墙与动画                   |
| 应用状态 | 轻量 TypeScript store                              | 当前可见状态、命令调度、异步保存状态；不直接执行 SQL   |
| 绘图适配 | `@excalidraw/excalidraw`                           | 活动场景编辑、元素序列化、静态 SVG/Canvas 导出         |
| 本地存储 | SQLite                                             | 便利贴元数据、场景 JSON、完成档案、设置、迁移          |
| 测试     | Vitest + Testing Library + Playwright + Rust tests | 领域规则、适配器契约、关键交互与桌面冒烟验证           |

选择 Tauri 2 的直接原因是：当前官方能力已经覆盖系统托盘、全局快捷键、自启动和 SQLite；应用仍可用 React 集成 Excalidraw，同时避免把桌面生命周期交给前端页面。

### 3.2 Excalidraw 的使用边界

不把整个产品实现成一个 Excalidraw 文件，也不为墙上的每张便利贴长期挂载一个完整 Excalidraw 实例。

推荐方案：

- 墙面背景最多挂载一个活动绘图实例；
- 当前正在检视的便利贴挂载一个活动绘图实例；
- 墙上未激活的便利贴使用导出的 SVG 或 Canvas 快照显示；
- 随手贴文字通过绘图适配器转换成普通文字元素；
- SQLite 保存应用自己的数据包络和 Excalidraw 场景 JSON；
- 产品层只认识 `DrawingScene`，不直接依赖 Excalidraw 元素类型。

这样可以避免便利贴数量增加时创建大量编辑器实例，也能在 Excalidraw 无法满足有限画布、工具裁剪或交互要求时，只替换绘图适配层。

## 4. 架构分层

```text
┌─────────────────────────────────────────────────────┐
│ UI / Features                                       │
│ 随手贴、当前墙、检视模式、完成墙、拖拽出口、动画       │
└───────────────────────┬─────────────────────────────┘
                        │ 用户意图 / ViewModel
┌───────────────────────▼─────────────────────────────┐
│ Application                                         │
│ CaptureNote、MoveNote、EditNote、Complete、Delete   │
│ 命令编排、事务边界、自动保存队列                       │
└───────────────────────┬─────────────────────────────┘
                        │ 端口接口
┌───────────────────────▼─────────────────────────────┐
│ Domain                                              │
│ Note、Wall、ArchiveItem、NoteSize、位置与状态不变量   │
│ 纯 TypeScript，不依赖 React/Tauri/SQLite/Excalidraw  │
└───────────────────────┬─────────────────────────────┘
                        │ Repository / Drawing / OS ports
┌───────────────────────▼─────────────────────────────┐
│ Adapters                                            │
│ SQLite、Excalidraw、Tauri IPC、系统时钟、ID 生成       │
└───────────────────────┬─────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────┐
│ Desktop Shell (Rust/Tauri)                          │
│ 窗口、托盘、快捷键、自启、单实例、退出与焦点           │
└─────────────────────────────────────────────────────┘
```

### 4.1 依赖规则

- `domain` 不得导入 React、Tauri、SQLite 或 Excalidraw；
- `application` 只依赖 `domain` 与端口接口；
- `features` 通过应用命令改变状态，不直接写数据库；
- SQL 只能出现在 SQLite 适配器与迁移文件中；
- Excalidraw 类型只能出现在绘图适配器内部；
- Rust 层不实现便利贴业务规则，只管理操作系统能力；
- 完成与删除必须经过应用服务，不能由动画组件自行修改数据。

用 ESLint 的 restricted imports 或依赖边界检查固定这些规则，防止项目增长后层级反向依赖。

### 4.2 推荐目录

```text
songtie/
├── product.md
├── implementation-plan.md
├── package.json
├── src/
│   ├── app/                    # 组合根、路由、窗口入口
│   ├── domain/                 # 纯模型与不变量
│   ├── application/            # 用例、端口、保存调度
│   ├── adapters/
│   │   ├── drawing-excalidraw/
│   │   ├── persistence-sqlite/
│   │   └── desktop-tauri/
│   ├── features/
│   │   ├── quick-capture/
│   │   ├── active-wall/
│   │   ├── note-inspector/
│   │   ├── completion-wall/
│   │   └── note-exits/
│   ├── ui/                     # 无业务含义的视觉组件
│   └── test-support/           # 内存适配器、builders、fixtures
├── src-tauri/
│   ├── src/                    # Tauri 生命周期和系统集成
│   ├── migrations/             # SQLite 迁移
│   └── capabilities/           # 最小权限声明
└── tests/
    ├── contract/
    ├── integration/
    └── e2e/
```

## 5. 核心领域模型

### 5.1 Note

```ts
type Note = {
  id: NoteId;
  size: 'small' | 'medium' | 'large';
  position: { x: number; y: number; z: number };
  scene: DrawingScene;
  passiveDate: string | null;
  revision: number;
  createdAt: string;
  updatedAt: string;
};
```

主要不变量：

- 尺寸只能是小、中、大；
- 当前便利贴必须位于有限墙面坐标系内；
- 场景内容必须完整落在便利贴边界内；
- 快捷输入超过大号纸张可见容量时，CaptureNote 必须拒绝创建；
- 日期只是字符串化的被动属性，不产生调度任务；
- 每次写入递增 `revision`，旧保存不得覆盖新内容。

### 5.2 Wall

```ts
type Wall = {
  id: 'active';
  bounds: { width: number; height: number };
  scene: DrawingScene;
  revision: number;
};
```

系统中只有一个活动墙。墙面涂鸦与便利贴位置分别保存，避免修改背景场景时重写所有便利贴。

墙面边界取首次运行时主屏幕的实际全屏逻辑尺寸。主窗口打开当前墙时直接进入全屏，墙面不使用额外的 1920×1080 设计基准，不提供平移、缩放、黑边区域或屏幕之外的隐藏空间。

### 5.3 CompletedNote

```ts
type CompletedNote = {
  id: NoteId;
  size: Note['size'];
  scene: DrawingScene;
  completedOn: string; // YYYY-MM-DD
  completedOrder: number;
};
```

完成操作在同一事务中：从活动便利贴集合移除，写入完成集合。完成墙不保存原墙位置，也不派生统计信息。

删除操作从活动领域中直接移除记录，不提供短时撤销、回收站或恢复领域模型。底层恢复日志属于数据维护设施，不改变产品中的永久删除语义。

### 5.4 DrawingScene 包络

```ts
type DrawingScene = {
  schemaVersion: 1;
  engine: 'excalidraw';
  engineVersion: string;
  payload: unknown;
};
```

应用自有包络用于迁移和校验。不要把每个 Excalidraw 元素拆成数据库表，也不要让数据库结构与第三方内部字段逐项绑定。

## 6. 持久化与一致性

### 6.1 首版数据库结构

```text
active_wall
  id = 'active', width, height, scene_json, revision, updated_at

notes
  id, size, x, y, z, scene_json, passive_date,
  revision, created_at, updated_at

completed_notes
  id, size, scene_json, completed_on, completed_order

recovery_log
  id, event_type, entity_id, payload_json, occurred_at

settings
  key, value_json
```

### 6.2 写入规则

- 创建、移动、调整尺寸、完成和删除立即使用事务写入；
- 连续绘画通过短暂 debounce 合并写入；
- pointer-up、失焦、关闭检视、隐藏窗口和应用退出前强制 flush；
- 同一场景使用串行写队列，新的 revision 不得被旧异步任务覆盖；
- 数据库迁移从第一版开始使用版本号并在事务中执行；
- 应用启动先完成迁移与数据校验，再开放编辑；
- 单实例插件阻止两个进程同时修改同一个数据库。

`recovery_log` 是只追加的便利贴生命周期日志，只允许 `note_created` 与 `note_deleted` 两类事件。创建事件保存初始快照，删除事件保存删除前的最终完整快照；移动、改字、绘画、日期变化和层级变化不写入日志。日志默认永久保留，只有直接维护原始数据库或清除整个应用数据目录才会移除。

产品层删除使用同一数据库事务：先把完整便利贴快照写入 `recovery_log`，再从 `notes` 删除。创建便利贴时，活动记录与 `note_created` 日志也必须在同一事务写入。应用层不定义 RestoreDeletedNote 用例，普通 Repository 不提供查询生命周期日志或已删除内容的方法。恢复只能通过直接访问原始数据库或独立、非产品入口的维护手段完成。

### 6.3 数据恢复原则

首个可用版本至少验证：

- 应用强制结束后，最近一次已确认操作仍存在；
- 迁移失败时旧数据库保持可打开状态；
- 场景 JSON 无法解析时，隔离损坏记录而不是阻止整个应用启动；
- 删除的产品语义仍是永久删除，技术备份策略必须在实现前单独与用户确认。

## 7. 窗口与桌面生命周期

建议使用两个实际窗口和一个主窗口路由：

1. `quick-capture`：小型、置顶、无复杂工具，快捷键唤出；
2. `main`：全屏窗口，在当前墙与完成陈列墙之间切换。

Rust/Tauri 层负责：

- 开机自启；
- 静默启动到托盘；
- 注册全局快捷键；
- 托盘菜单：打开当前墙、随手贴、完成陈列墙、退出；
- 关闭主窗口时隐藏而非退出；
- 明确点击“退出”时 flush 数据并结束进程；
- 保证单实例；
- 将窗口唤出、聚焦和隐藏封装为桌面命令。

Linux 下托盘图标的部分点击事件存在平台限制，因此阶段 0 必须在真实桌面环境验证托盘菜单，而不能只依赖浏览器测试。

## 8. 绘图与渲染策略

### 8.1 活动编辑

- 墙面编辑器绑定墙面 `DrawingScene`；
- 检视模式绑定当前便利贴 `DrawingScene`；
- 通过 Excalidraw `onChange` 进入应用保存队列；
- 使用受控 UI 只暴露已批准工具；
- 禁止协作、素材库、链接嵌入、无限缩放等非目标能力；
- 编辑容器裁剪到墙面或便利贴边界。

### 8.2 静态显示

- 未激活便利贴从场景导出 SVG 或 Canvas；
- 快照按 `noteId + revision` 缓存；
- 场景 revision 改变时才重新生成；
- 完成墙复用同一只读渲染器；
- 快照只是缓存，数据库中的场景 JSON 才是事实来源。

### 8.3 必须先验证的风险

Excalidraw 默认是无限画布，因此必须用独立技术实验验证：

- 能否固定视口并禁止用户离开有限区域；
- 能否限制元素不越过便利贴边界；
- 能否精简工具栏而不 fork 上游代码；
- 中文字体、文字缩放和 SVG 导出是否视觉一致；
- 一张墙面场景加若干静态便利贴时性能是否稳定；
- 检视模式挂载与卸载编辑器是否造成焦点或快捷键冲突。

如果关键约束只能通过长期维护 Excalidraw fork 才能实现，阶段 0 立即停止正式开发，改为评估基于 Canvas/SVG 与 Rough.js 的最小自有绘图适配器。领域层、应用层和数据库设计保持不变。

## 9. 分阶段执行计划

每个阶段必须满足三个条件才可结束：功能可在真实桌面运行、自动化验证通过、数据重启后仍然正确。

### 阶段 0：决策与高风险技术实验

目标：在搭建正式业务代码前，消除会推翻架构的风险。

执行：

1. 固定并记录 Ubuntu 22.04 / Linux x86_64 运行基线；
2. 创建一次性 Tauri 实验壳；
3. 验证托盘菜单、全局快捷键、静默启动、自启动和窗口聚焦；
4. 嵌入一个 Excalidraw 编辑器，限制到固定矩形；
5. 创建文字、移动、缩放、画线并保存场景 JSON；
6. 重新载入场景并导出 SVG；
7. 用中文、四字短句、多行文字和简单图形做视觉测试；
8. 同屏渲染至少 100 张静态便利贴快照，记录交互流畅度；
9. 记录采用 Excalidraw 或最小自有绘图适配器的结论。

验收：

- 快捷键可以可靠显示并聚焦小窗口；
- 托盘菜单可以打开两个入口并真正退出；
- 场景保存、重启、恢复后视觉一致；
- 固定边界可以落实且无需维护上游 fork；
- 静态便利贴方案没有明显卡顿。

停止条件：以上任一核心能力失败时不进入阶段 1，先替换对应技术选择。

### 阶段 1：可运行骨架与架构边界

目标：建立之后每个切片都能复用的最小工程骨架。

执行：

1. 初始化 Tauri 2 + React + TypeScript + Vite；
2. 建立 domain/application/adapters/features 目录；
3. 配置格式化、类型检查、Lint、Vitest 和 Rust 检查；
4. 建立 SQLite v1 迁移；
5. 建立内存 Repository 和 SQLite Repository 契约测试；
6. 建立 Tauri 窗口端口的 fake adapter；
7. 添加依赖边界检查；
8. 提供统一命令：`check`、`test`、`test:contract`、`tauri:dev`。

验收：

- 空应用能静默启动到托盘；
- 数据库自动创建并完成迁移；
- 内存与 SQLite 适配器通过同一套契约测试；
- CI/本地一条命令可以完成全部静态检查和单元测试。

停止条件：架构边界和迁移测试通过，不加入任何产品动画或额外设置。

### 阶段 2：随手贴纵向切片

目标：第一次交付真实产品价值——写下想法并立即离开。

执行链路：

```text
全局快捷键
→ quick-capture 窗口聚焦
→ 固定输入框打字
→ 计算小/中/大尺寸并显示结果
→ CaptureNote 用例
→ SQLite 保存
→ 窗口隐藏
```

首版只支持纯文字，不支持 Markdown 和绘图。

确认时如果文字超过大号纸张的可见容量，应用保留输入内容并提示用户删减；在内容能够完整显示之前，不创建便利贴。系统不自动拆分、不缩小字体，也不产生隐藏溢出。

验收：

- 无论主窗口是否打开，快捷键都能唤出输入框；
- 输入后按确认，窗口立即隐藏；
- 空内容不创建便利贴；
- 自动尺寸是确定性的；
- 超过大号容量时无法确认，删减到可容纳后才能创建；
- 容量校验失败不会清空或修改用户已经输入的内容；
- 应用重启后便利贴仍存在；
- 快捷输入期间不加载或展示整面墙。

自动化验证：尺寸规则单元测试、CaptureNote 用例测试、SQLite 契约测试、快捷窗口交互测试。

### 阶段 3：有限当前墙与直接移动

目标：打开唯一当前墙并看到、移动已记录的便利贴。

执行：

1. 建立固定墙面坐标系；
2. 读取并显示所有活动便利贴；
3. 使用静态场景渲染器显示完整文字；
4. 支持拖拽、z-index 和墙面边界约束；
5. 位置变化通过 MoveNote 用例保存；
6. 重启后恢复相同布局；
7. 托盘菜单可以打开当前墙。

验收：

- 便利贴不能被拖出墙面；
- 内容全量显示，无摘要、折叠和内部滚动；
- 移动后重启，位置与层级不变；
- 墙面不会平移成无限画布；
- 主窗口进入后整块屏幕就是墙面，不出现设计画布黑边；
- 100 张便利贴仍可顺畅拖动。

### 阶段 4：检视模式与便利贴自由编辑

目标：把文字便利贴渐进为一张可自由编辑的小画布。

执行：

1. 点击便利贴进入居中检视；
2. 等比放大，背景变暗并轻微模糊；
3. 挂载唯一活动便利贴编辑器；
4. 快捷输入文字恢复为普通可移动文字对象；
5. 支持文字、自由线、箭头和基础图形；
6. 关闭时保存并生成新静态快照；
7. 缩回原位置，墙面显示更新后的完整内容。

验收：

- 进入和退出不会改变便利贴原位置或尺寸；
- 文字、图形和留白在编辑前后视觉一致；
- 内容不能越过纸张边界；
- 不出现“展开全文”或滚动容器；
- 重启后场景与静态快照一致。

### 阶段 5：墙面涂鸦层

目标：让用户用墙面本身表达分区和逻辑关系。

执行：

1. 在便利贴下方加入单一墙面绘图层；
2. 支持画笔、橡皮、文字、箭头和简单图形；
3. 建立选择/移动便利贴与绘制墙面的明确输入模式；
4. 保存墙面场景，不重写便利贴数据；
5. 验证遮挡关系和重启恢复。

验收：

- 墙面元素始终位于便利贴下方；
- 墙面涂鸦和便利贴可以独立保存；
- 工具切换不会误移动便利贴或误画线；
- 有限墙面边界始终有效。

### 阶段 6：尺寸、被动日期与边缘吸附

目标：补齐已确定的物理规则，不增加管理系统。

执行：

1. 固化三种纸张尺寸和视觉比例；
2. 支持全屏创建大号便利贴；
3. 支持在允许条件下切换固定尺寸；
4. 添加纯展示日期；
5. 添加轻微边缘吸附；
6. 吸附后仍保存独立位置，不建立组合实体。

验收：

- 不存在自由拉伸或第四种尺寸；
- 日期不注册后台任务，不触发颜色或提醒；
- 吸附只发生在明确阈值内，拖离后立即解除；
- 相邻便利贴仍可单独移动、完成和删除。

### 阶段 7：完成、删除与完成陈列墙

目标：实现两种不同的告别，并保证数据语义正确。

执行：

1. 拖动便利贴时显示垃圾桶和完成入口，松开鼠标前可以移出目标取消；
2. 完成操作执行数据库事务并记录本地日期；
3. 删除操作在同一事务中写入底层恢复日志并永久移除活动记录；
4. 建立独立完成陈列墙路由；
5. 按完成日期展示只读便利贴；
6. 点击完成项时居中放大，只读查看；
7. 先使用简化动画，完整特效留到阶段 9。

验收：

- 完成事务不能产生“活动墙和完成墙同时存在”或“两边都不存在”的中间状态；
- 日期只精确到天；
- 完成墙没有数量、连续天数、分析和总结；
- 删除内容不会进入完成墙或普通回收站；
- 程序界面和应用用例不存在恢复入口，但原始恢复日志包含删除前的完整快照；
- 生命周期日志只出现创建和删除事件，不因移动、编辑或绘画增长；
- 完成项不能被编辑。

### 阶段 8：桌面生命周期与可靠性收口

目标：让应用真正适合长期常驻，而不是只能在开发窗口运行。

执行：

1. 完成开机自启；
2. 关闭窗口改为隐藏，托盘“退出”才结束进程；
3. 退出前 flush 所有保存队列；
4. 处理快捷键冲突和注册失败；
5. 处理数据库损坏记录隔离；
6. 验证崩溃恢复和数据库迁移；
7. 验证底层恢复日志，并确认本地备份策略；应用界面不加入误删除撤销。

验收：

- 开机后静默常驻；
- 连续唤出/隐藏不会产生重复窗口；
- 强制结束应用后，已确认操作不会大面积丢失；
- 第二次启动只聚焦已有实例；
- 无网络时全部功能正常。

### 阶段 9：手感、动画与视觉验收

目标：在功能与数据可靠后，兑现“像 iPad、像游戏查看物品、删除和完成都很爽”的体验。

执行：

1. 确定中文手写感字体与替代字体；
2. 调整墙面材质、纸张颜色、阴影和轻微不规整感；
3. 完成检视模式的运动曲线和模糊强度；
4. 完成删除动画；
5. 完成盖章、庆祝和飞入完成墙动画；
6. 调整边缘吸附的阻尼与反馈；
7. 做键盘、鼠标和触控板的完整体验走查。

验收：

- 动画不会先于事务成功误导用户；
- 动画中断后数据状态仍然唯一且正确；
- 快捷记录从唤出到保存没有多余步骤；
- 使用后感受是放松和直接，而不是任务管理压力。

停止条件：达到 `product.md` 的体验判断标准后停止扩展，不加入新的管理功能。

## 10. 持续验证策略

### 10.1 每次提交的快速门禁

```text
格式化
→ TypeScript 类型检查
→ ESLint 与架构边界
→ Vitest 领域/应用单元测试
→ Rust fmt + clippy + test
```

### 10.2 每个阶段的集成门禁

```text
SQLite 迁移测试
→ Repository 契约测试
→ React 关键交互测试
→ 浏览器级 Playwright 流程
→ 真实 Tauri 桌面冒烟测试
→ 重启恢复检查
```

### 10.3 必须长期保留的回归场景

1. 快捷键记录四个中文字，生成小号便利贴并在重启后出现；
2. 编辑便利贴，移动文字并画箭头，退出再进入后视觉不变；
3. 将便利贴拖到墙边，不能越界；
4. 两张便利贴边缘吸附后仍能分别移动；
5. 添加过去日期，不出现提醒、红色逾期或系统通知；
6. 完成便利贴后，活动墙消失、完成墙出现且日期正确；
7. 删除便利贴后，不出现在活动墙和完成墙；底层恢复日志写入完整快照；
8. 强制结束应用并重启，最后已确认状态保持一致；
9. 完成墙永远不出现统计、连续天数或分析文案。

## 11. 关键技术注意点

### 11.1 不让第三方场景格式成为领域模型

Excalidraw 数据只通过 DrawingAdapter 进入系统。升级依赖时先用固定场景 fixtures 做恢复和导出回归测试，再更新锁文件。

### 11.2 不同时挂载大量绘图编辑器

墙上便利贴使用静态快照；只有墙面和当前检视对象需要活动编辑能力。否则输入事件、字体加载和渲染成本会随便利贴数量快速增加。

### 11.3 自动保存必须处理乱序

绘图 `onChange` 触发频繁。只 debounce 不足以防止旧异步保存晚于新保存完成。保存队列必须按场景串行，并用 revision 防止旧写覆盖新写。

### 11.4 动画与数据事务分离

完成/删除流程应为：

```text
用户确认拖入出口
→ 冻结该便利贴交互
→ 提交领域命令与数据库事务
→ 成功后播放最终离场动画
→ 更新视图
```

事务失败时便利贴回到原位并明确显示保存失败，不能播放“已经删除/完成”的假象。删除日志写入和活动记录删除必须属于同一事务。

### 11.5 有限空间必须是领域规则

边界不能只靠 CSS `overflow: hidden`。MoveNote、ResizeNote 和场景保存都要验证坐标，避免数据实际位于墙外但只是暂时看不见。

### 11.6 先实现手感所需最小工具集

首个绘图版本只需要文字、自由线、箭头、矩形、圆形、选择和橡皮。每增加一个工具，都必须能解释它如何帮助表达，而不是因为 Excalidraw 已经提供。

## 12. 仍需逐项讨论的技术决策

依赖顺序如下，每次只解决一个：

1. Excalidraw 集成实验是否满足有限画布要求；
2. 三种纸张的像素尺寸与比例；
3. 本地备份是否存在、用户是否可见；
4. 完成陈列墙的具体排列规则；
5. Markdown 的最小支持范围；
6. 快捷键冲突时的退路。

以上决策可以使用 grill-me 逐项确认。任何决策都不得提前引入同步、账号、多墙、智能整理或插件体系。

## 13. 官方技术依据

- Tauri 2 系统托盘：https://v2.tauri.app/learn/system-tray/
- Tauri 全局快捷键：https://v2.tauri.app/plugin/global-shortcut/
- Tauri 自启动：https://v2.tauri.app/reference/javascript/autostart/
- Tauri SQLite 与迁移：https://v2.tauri.app/plugin/sql/
- Excalidraw React 集成：https://docs.excalidraw.com/docs/@excalidraw/excalidraw/integration/
- Excalidraw Props 与 `onChange`：https://docs.excalidraw.com/docs/@excalidraw/excalidraw/api/props/
- Excalidraw SVG/Canvas 导出：https://docs.excalidraw.com/docs/@excalidraw/excalidraw/api/utils/export/
